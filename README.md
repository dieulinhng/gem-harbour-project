# Gem Harbour Project
## 1. Tạo Virtual Environment (venv)
```bash
uv venv myenv --python 3.14.6
```
## 2. Kích hoạt Virtual Environment (venv)
Trên macOS / Linux:
```bash
source myenv/bin/activate
```

Trên Windows:
```bash
source myenv/Scripts/activate
```